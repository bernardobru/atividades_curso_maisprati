/**
 * 
 * Imprime no console um número 10 vezes
 * @param {number} a Número a ser imprimido
 * @example
 * printIntegerTenTimes(10);
 * // output: 10, 10, 10, 10, 10, 10, 10, 10, 10, 10 
 */
export default function printIntegerTenTimes(a) {
    for (let i = 0; i < 10; i++) {
        console.log(a);
    }
}